name = "beacon-cm"
main = "_worker.js"
compatibility_date = "2025-11-04"
keep_vars = true

[[kv_namespaces]]
binding = "KV"
#id = ""				# 在 Cloudflare Dashboard 绑定，无需写在这里

[[d1_databases]]
binding = "DB"
database_name = "beacon-tunnel-db"
# database_id = ""   # 在 Cloudflare Dashboard → D1 → beacon-tunnel-db 中复制 ID 填入